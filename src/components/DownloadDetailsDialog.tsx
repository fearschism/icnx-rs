// Temporary stub to satisfy imports. Replace with real dialog implementation.
import React from 'react';

interface Props {
	isOpen?: boolean;
	onClose?: () => void;
	sessionId?: string;
}

const DownloadDetailsDialog: React.FC<Props> = () => null;

export default DownloadDetailsDialog;
